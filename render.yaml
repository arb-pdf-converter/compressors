services:
  - type: web
    name: pdf-compressor-service
    env: docker  # ✅ Changed to Docker
    dockerfilePath: ./Dockerfile  # ✅ Custom Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: pdf-storage
      mountPath: /app/uploads
      sizeGB: 2
